# This file should be included at the top of every makefile that compiles a chombo-discharge appliaction. It defines the necessary Chombo variables
# Chombo variables and aldo includes all paths that contain chombo-discharge source files. 

include $(CHOMBO_HOME)/mk/Make.defs
include $(DISCHARGE_HOME)/Lib/Definitions.make
